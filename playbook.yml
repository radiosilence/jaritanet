---
- name: Ensure all configurations are applied
  hosts: all
  become: true
  vars_files:
    - group_vars/all.yml

  roles:
    - common
    - users
    - syncthing
    - microk8s
    - tailscale
    - samba
