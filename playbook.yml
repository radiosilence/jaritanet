---
- name: Ensure all configurations are applied
  hosts: all
  become: true
  vars_files:
    - group_vars/all.yml

  roles:
    - common
    - users
    - tailscale
    - syncthing
    - microk8s
    - samba
