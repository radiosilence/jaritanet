# SSH Keys
*.pem
*.key
id_*

# Logs
*.log

# Environment files
.env

# OS generated files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

mise.local.toml

node_modules

apps/*/target

# Where CI unpacks the compiled binary for the container build, and where a
# local reproduction of that build has to put it. Same name as the crate, so
# only these two can be spelled out rather than globbed.
/apps/auth/auth


# harness-managed agent worktrees
.claude/worktrees/

# Written by `mise run auth`; holds a GitHub Packages token.
.npmrc.auth
