pre-commit:
  parallel: true
  commands:
    lint:
      run: npx oxlint --fix {staged_files}
      stage_fixed: true
    format:
      run: npx oxfmt --write {staged_files}
      stage_fixed: true
    typecheck-infra:
      run: npm run typecheck:infra
