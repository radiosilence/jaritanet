name: Publish Files Container

permissions:
  contents: read
  packages: write

on:
  push:
    branches:
      - main
    tags:
      - v*
    paths:
      - "containers/files/**"
  pull_request:

jobs:
  publish-web:
    uses: radiosilence/blit-workflows/.github/workflows/build-publish-container.yml@main
    with:
      working_directory: "./containers/files"
    secrets:
      PASSWORD: ${{ secrets.GITHUB_TOKEN }}
