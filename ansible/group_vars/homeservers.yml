---
# Specific configuration for homeservers
microk8s:
  enabled: true
  addons:
    - dns
    - storage
    - traefik

syncthing:
  enabled: true
  config_dir: "/home/{{ username }}/.config/syncthing"

samba:
  enabled: true
