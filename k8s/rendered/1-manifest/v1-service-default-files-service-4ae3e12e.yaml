apiVersion: v1
kind: Service
metadata:
  annotations:
    pulumi.com/autonamed: "true"
  name: files-service-4ae3e12e
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    name: files
