/**
 * A bare tag rather than a full reference, because `ServiceArgsSchema` wants
 * the repository and the tag apart.
 *
 * Rewritten in place by the version updater; see `.github/tracked-versions.yml`.
 */
export const VERSIONS = {
  navidrome: "0.63.2",
} as const;
